// DashboardClient is no longer used.
// All routing is now handled by the Next.js App Router under /dashboard/*/page.tsx
// This file is kept temporarily to avoid import errors during transition.
// It can be safely deleted once the build is confirmed green.

export default function DashboardClient() {
    return null;
}
